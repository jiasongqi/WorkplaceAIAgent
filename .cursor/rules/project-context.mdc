---
description: WorkPilot 项目上下文 — Agent 平台开发规范与代码地图
globs:
alwaysApply: true
---

# WorkPilot 项目上下文

全场景职场 AI 智囊平台。Java 21 + Spring Boot 3.4 + Spring AI + Vue 3。

## 代码地图

开发前先查阅 `.cursor/codemaps/` 目录：
- `00-INDEX.md` — 全量索引
- `03-backend-agents.md` — Agent 路由与子 Agent
- `04-backend-api.md` — REST API 端点
- `05-frontend.md` — 前端路由与 API
- `06-features-by-layer.md` — L0-L33 功能层映射

## 架构约束

1. **分层调用**：Controller → AppService → Agent/Domain，禁止 Controller 直接调用 Agent
2. **OrchestratorAgent** 是主控路由中枢，子 Agent 不互相调用
3. **SSE 鉴权**：EventSource 不支持 Header，token 通过 URL 参数传递
4. **新增 Agent**：需同时改 Agent 类、AgentConfig、OrchestratorAgent、NLU 意图、permissions YAML
5. **新增工具**：创建 Tool 类 → ToolRegistration 注册
6. **新增技能**：在 `src/main/resources/skills/` 添加 YAML 即可

## 关键路径

- 主聊天入口：`service/OrchestratorAppService.java` → `agent/OrchestratorAgent.java`
- 前端主页面：`yu-ai-agent-frontend/src/views/CareerAdvisor.vue`
- API 客户端：`yu-ai-agent-frontend/src/api/index.js`
- Agent 装配：`config/AgentConfig.java`
- 主配置：`src/main/resources/application.yml`

## 命名与风格

- 后端包：`com.yupi.yuaiagent.{module}`
- AppService 后缀：`*AppService.java`
- Agent 后缀：`*Agent.java`
- Controller 只做 HTTP 适配，不含业务逻辑
- 匹配现有代码风格，最小化 diff

## 启动

```bash
mvn spring-boot:run          # 后端 :8123
cd yu-ai-agent-frontend && npm run dev  # 前端 :3000
```

## 详细文档

- 完整 Wiki：`docs/WIKI.md`
- 功能分层：`docs/FEATURES.md`
- NLU 设计：`docs/nlu-layer-design-v4.2.md`
