- 所有视图组件均在`onBeforeUnmount`或`onMounted`生命周期中管理`EventSource`连接的创建与关闭，防止内存泄漏。
- AI消息内容统一通过`renderMarkdown`函数处理，支持HTML标签注入以实现富文本展示。
- 输入框均绑定`@keydown.enter.exact.prevent`以拦截默认换行行为并触发发送，同时支持`Shift+Enter`进行换行。
- 聊天容器通过`ref`获取DOM引用，并利用`nextTick`在消息更新后自动滚动至底部。