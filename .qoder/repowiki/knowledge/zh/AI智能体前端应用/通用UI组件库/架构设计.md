- 采用 Vue 3 `<script setup>` 语法糖构建无状态或轻量状态组件，如 `AiAvatarFallback.vue` 和 `AppFooter.vue`。
- `AppLayout.vue` 作为核心布局容器，集成 `vue-router` 的 `<router-view>` 和 `<router-link>`，定义侧边栏导航与主内容区的结构。
- 样式均采用 `<style scoped>` 实现组件级 CSS 隔离，其中 `AppLayout.vue` 依赖 CSS 变量（如 `--sidebar-w`）进行主题化配置。