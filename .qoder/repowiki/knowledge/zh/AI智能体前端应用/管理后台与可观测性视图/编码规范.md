- 视图组件统一采用 `<script setup>` 语法糖定义逻辑，配合 scoped CSS 实现样式隔离。
- API 请求统一通过 `src/api/index.js` 导出的方法发起，利用 Axios 拦截器自动注入 Bearer Token 并处理 401 自动重登录。
- 实时数据更新（如 Trace 执行状态）采用 `setInterval` 轮询接口的方式，并在组件卸载时通过 `onBeforeUnmount` 清除定时器。
- 状态标识（如 SUCCESS, RUNNING, FAILED）在 UI 中通过动态 class 绑定实现颜色与图标的统一视觉反馈。