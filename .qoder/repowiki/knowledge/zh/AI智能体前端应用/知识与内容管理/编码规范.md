- 统一在组件顶部设置包含“返回”按钮、标题和状态计数的 Header 区域。
- 使用 `localStorage.getItem('token')` 或 Axios 拦截器处理 API 请求的身份认证。
- 采用 `ref` 和 `reactive` 管理本地状态，并通过 `onMounted` 钩子触发初始数据加载。
- 在展示列表时提供加载状态 (`loading`) 和空数据占位符 (`placeholder`) 的用户反馈。